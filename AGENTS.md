# RADAR AI 开发入口

本文件供 AI 和自动化开发工具在仓库根目录自动发现。它只定义资料定位和读取顺序；架构、数据、安全、协作等规则以其对应的正式文档为唯一来源。

## 开始改动前

1. 确认全局需求引用 `<开发者账号>/REQ-YYYYMMDD-NNN`，读取 `docs/requirements/<开发者账号>/<REQ>/requirement.md` 与 `ai-task-scope.yaml`，并完成影响分析识别出的相关文档。需求、任务范围、相关文档或目标模块缺失时停止改动；不得先改代码再补文档。
2. 在 [`governance/modules.yaml`](governance/modules.yaml) 确认目标模块、Owner、可修改路径、依赖和公开契约。
3. 读取本文件所在目录至目标文件路径上最近的 `AGENTS.md`；较深目录仅补充该目录特有的实现约定。
4. 按下表读取适用的唯一事实源，再开始设计或修改。

| 问题或变更类型 | 必读来源 |
| --- | --- |
| 架构、跨模块访问、数据、接口、迁移、权限、审计、外网、质量 | [docs/governance/PROJECT-RULES.md](docs/governance/PROJECT-RULES.md) |
| AI 准入、敏感信息、任务范围和完成报告 | [docs/governance/AI-CODING-RULES.md](docs/governance/AI-CODING-RULES.md) |
| 分支、PR、审批、CI | [docs/governance/GITHUB-RULES.md](docs/governance/GITHUB-RULES.md) |
| 模块职责与物理目录 | [docs/architecture/MODULES.md](docs/architecture/MODULES.md) |
| UI、组件、响应式与可访问性 | [DESIGN.md](DESIGN.md) |
| 数据库搬迁、备份与恢复 | [MIGRATION.md](MIGRATION.md) |

## 定位约定

- 以 `modules.yaml` 判断模块边界和公开契约；不要从目录结构或相似实现推断权限。
- 以 `ai-task-scope.yaml` 判断可写、只读和禁止路径；不要扩大任务范围。
- 需求目录说明业务结果和验收条件；架构决策在 `docs/architecture/decisions/` 中查找。
- 完成时按 AI Coding 规约如实报告实际文件、验证、风险和回退方式。

## 高频遗漏提醒

开始编码前，先在需求或交付说明中对下列项目写出“适用”或“不适用”及原因；详细规则仍以对应正式规约为准，可参考 [需求改动示例](docs/requirements/EXAMPLES.md)。

| 变更类型 | 必须先确认的事项 |
| --- | --- |
| 需求与范围 | 需求、任务范围、验收、影响分析和相关文档是否已经完成且为 `ready`；是否混入无关重构、依赖升级或其他需求 |
| 新增或调整填写、展示、校验字段 | 是否需要在系统设置的输入项配置中注册范围、字段、布局和状态规则；必填项是否同步种子与 mock 数据 |
| 枚举、默认值或可导入字段 | 合法值、空值与默认值、服务端创建/更新/导入校验、非法行错误反馈是否一致；前端下拉不能替代服务端校验 |
| 新增交付件、模板、上传下载或交付校验 | 是否需要在系统设置的交付件配置中注册定义、状态规则和模板版本 |
| 状态、动作或流程 | 当前状态、允许角色、前置条件、失败保持状态和审计是否明确 |
| 只读展示、指标或统计 | 是派生读取还是新业务字段；是否会写回；统计口径是否需要统一 |
| 公共契约、`platform`、`shared` | 是否属于公开能力变更，公开入口、兼容、审批、批量读取和回归应如何说明 |
| 多模块或配置/平台改动 | `assignment.module` 是否为一个登记主模块；所有受影响模块、直接写入原因、Owner 审批和公开契约是否已记录 |
| API、导入导出或接口字段 | DTO 与字段白名单、历史接口兼容、错误反馈、数据范围和脱敏是否明确 |
| 数据库与历史数据 | 是否真的需要迁移；双端迁移、存量兼容、索引、回退或补偿是否明确 |
| 权限、审计、附件、外网 | 服务端 RBAC、数据范围、实体授权、审计、附件访问控制和外网白名单是否有影响 |
| 页面与共享组件 | 是否复用现有表格、筛选、状态、权限和附件能力；加载、空态、失败、无权限、移动端和主题是否覆盖 |
| 分支、提交、PR | 当前分支是否与任务范围一致；需求引用是否始终使用 `<开发者账号>/REQ-YYYYMMDD-NNN`，分支是否满足 `<开发者账号>/REQ-YYYYMMDD-NNN-short-name` |
| 测试与完成记录 | 每条验收是否有实际命中的测试；测试筛选条件是否真的命中用例；完成记录是否已填测试结果、风险和回退 |
