-- ============================================================================
-- 文件：0006_role_default_theme.sql
-- 用途：为角色表增加"默认主题"标识。管理员可以配置每个角色进入系统后的初始默认主题。
-- 作者：hengguan
-- ============================================================================

ALTER TABLE role ADD COLUMN default_theme TEXT NOT NULL DEFAULT 'sky';
