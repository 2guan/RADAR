-- ============================================================================
-- 文件：0033_stage_section_layout.sql
-- 用途：为阶段内容分区增加详情布局位置（左侧 / 右侧 / 整行）。
-- 说明：默认左侧，兼容已存在的阶段分区与历史配置。
-- ============================================================================

ALTER TABLE stage_section ADD COLUMN layout_mode TEXT NOT NULL DEFAULT 'left';
