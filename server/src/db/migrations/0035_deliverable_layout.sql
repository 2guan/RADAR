-- ============================================================================
-- 文件：0035_deliverable_layout.sql
-- 用途：为公共交付件增加详情布局位置（左侧 / 右侧 / 整行）。
-- 说明：默认左侧，保持已存在交付件在详情页中的原有展示位置。
-- ============================================================================

ALTER TABLE deliverable_definition ADD COLUMN layout_mode TEXT NOT NULL DEFAULT 'left';
