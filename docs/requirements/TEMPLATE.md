---
document_type: ai_coding_requirement
template_version: "1.0"
requirement_id: "REQ-YYYYMMDD-001"
requirement_ref: "[开发者账号]/REQ-YYYYMMDD-001"
title: "[需求标题]"
status: "draft"
priority: "P2"
requester: "[需求人员]"
developer: "[研发人员]"
module: "[登记主模块]"
module_owner: "[模块 Owner]"
contains_confidential_information: false
external_access_required: false
internet_ai_coding_allowed: true
last_updated: "YYYY-MM-DD"
---

# [[开发者账号]/REQ-YYYYMMDD-001] [需求标题]

> 一份文档只描述一个可独立实现、测试和验收的细粒度需求。目录固定为 `docs/requirements/<开发者账号>/<REQ-YYYYMMDD-NNN>/`，全局引用固定为 `<开发者账号>/REQ-YYYYMMDD-NNN`。未涉及项填写“不涉及”。涉密需求不得提交给互联网 AI。

## 0. AI 执行约束

- 仅实现本次范围，不实现明确不做项；只修改任务范围允许路径。
- 优先复用既有组件、公开契约、权限、审计与附件能力；不得无关重构、替换技术栈或升级依赖。
- 需求、任务范围及影响分析识别出的配置、契约、迁移、权限审计、测试和回退等相关文档必须先完成并确认 `ready`，才可实施代码改动；实施中结论变化时先更新文档。
- 需求、代码与规约冲突时先报告；存在影响正确实现的未决问题时不得编码。

## 1. 目标与业务价值

- 要解决的问题：
- 预期结果：
- 业务价值：

## 2. 使用者与场景

| 使用者 | 触发条件 | 前置条件 | 操作场景 | 完成结果 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 3. 范围

### 本次要做

1. 

### 明确不做

1. 

- [ ] 可独立实现
- [ ] 可独立测试
- [ ] 可独立验收

## 4. 行为与业务规则

- 当前行为：
- 变更后行为：
- 失败行为：

| 规则编号 | 触发条件 | 业务规则 | 不满足时处理 |
| --- | --- | --- | --- |
| BR-001 |  |  |  |

## 5. 页面、数据与状态（涉及时填写）

| 页面/区域 | 入口 | 展示 | 动作 | 成功/失败反馈 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

| 字段 | 含义 | 必填/合法值/空值处理 | 脱敏示例 | 涉密 | 外网可见/可写 |
| --- | --- | --- | --- | --- |
|  |  |  |  | 否 | 不涉及 |

| 当前状态 | 动作/角色 | 条件 | 目标状态 | 失败保持 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 5A. 配置与交付影响分析

> 每项必须填写“适用”或“不适用”；“不适用”也必须说明理由。规则解释以 `docs/governance/PROJECT-RULES.md` 为准；填写方式参考 [需求改动示例](EXAMPLES.md)。

| 项目 | 适用？ | 结论、标识与验证证据 |
| --- | --- | --- |
| 输入项配置注册 | 是 / 否 | 配置范围、字段标识、布局/状态规则；新库种子、旧库幂等补齐键、管理员配置保留策略和回显验证；或不适用原因 |
| 交付件配置注册 | 是 / 否 | 配置范围、交付件标识、状态规则/模板版本；或不适用原因 |
| 种子与 mock 数据 | 是 / 否 | 必填项合法值、影响位置和正常场景验证；或不适用原因 |
| 服务端字段校验与导入导出 | 是 / 否 | 合法值、默认/空值、创建/更新/导入的统一校验；非法行字段、原值和错误反馈；或不适用原因 |
| 公共能力或跨模块契约 | 是 / 否 | 调用/新增的公开契约、兼容方式、审批；或不适用原因 |
| 数据库与历史数据 | 是 / 否 | SQLite/TDSQL/MySQL 8 影响、迁移、兼容和回退；或不适用原因 |
| 权限、审计、附件、外网 | 是 / 否 | 影响、服务端验证与测试；或不适用原因 |

## 6. 权限、审计与外网

| 角色 | 查看/新增/修改/动作 | 数据范围 | 内网/外网 |
| --- | --- | --- | --- |
|  |  |  | 内网 |

- 无权限处理：
- 审计要求：
- 外网开放场景、字段、动作、附件限制与禁止项：不涉及

## 7. 验收与脱敏示例

| 编号 | 类型 | Given | When | Then |
| --- | --- | --- | --- | --- |
| AC-001 | 正常 |  |  |  |
| AC-002 | 异常 |  |  |  |
| AC-003 | 权限 |  |  |  |
| AC-004 | 边界 |  |  |  |

```json
{ "example": "仅使用虚构或脱敏数据" }
```

禁止使用生产数据、真实账号/密钥、内网地址、真实日志、附件和截图作为示例。

## 8. 研发上下文

- 目标模块 / Owner / 基准分支：`module` 与任务范围 `assignment.module` 必须是 `modules.yaml` 的同一个登记主模块；`requirement_ref`、目录开发者、任务范围 `assignment.developer` 和分支前缀必须一致；分支必须为 `<开发者账号>/REQ-YYYYMMDD-NNN-short-name`；获批准的例外须同时写入需求与任务范围。
- 多模块协作：列出全部受影响模块、直接写入其内部实现的原因、公开契约、各 Owner 审批和 `public_capability_change.target_modules`；不得用逗号拼接 `assignment.module`。
- 允许与禁止修改路径：见 `ai-task-scope.yaml`。
- 必须复用的能力与公开契约：
- 接口契约（路径、方法、请求、响应、权限）：
- 数据库迁移、历史数据、SQLite/TDSQL/MySQL 8 兼容及回退：
- 必须执行的构建、单元、API、权限、外网白名单与回归测试：逐条关联 AC/BR 与测试文件或用例名；若使用筛选命令，写明其实际命中的用例：
- 风险、审批与未决问题：

## 9. 完成记录

- 修改文件与范围一致性：
- 配置与交付影响落实：逐项说明第 5A 节的适用结果、配置标识或不适用原因：
- 测试证据：列出实际运行命令、结果及覆盖的 AC/BR，不得以未命中功能用例的筛选命令代替：
- 已知风险：
- 发布验证与回退：

## 10. 需求准入

- [ ] 核心规则与验收标准无未决项
- [ ] 涉密和外网边界明确
- [ ] 配置与交付影响已逐项分析，并已记录适用范围或不适用原因
- [ ] `requirement_ref`、标题、目录、任务范围、开发者和分支一致
- [ ] 主模块为 `modules.yaml` 的单个键；多模块改动与 Owner 审批已记录
- [ ] 当前分支、需求编号与任务范围一致
- [ ] 所有示例已脱敏
- [ ] 互联网 AI 使用许可已明确
